# EXAMPLE ONLY. Copy outside Git and replace through an approved secret workflow.
apiVersion: v1
kind: Secret
metadata: {name: albireo-identity, namespace: albireo}
type: Opaque
stringData:
  node.key: REPLACE_OUTSIDE_GIT
  peers.bundle: REPLACE_OUTSIDE_GIT
